# Misc behaviors and XWayland — sourced from hyprland.conf.
misc {
    allow_session_lock_restore = true
    focus_on_activate          = true
}

xwayland {
    force_zero_scaling = true # X11 apps render sharp (native res) instead of blurry-scaled
}
